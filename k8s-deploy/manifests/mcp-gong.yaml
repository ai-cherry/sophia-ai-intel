apiVersion: apps/v1
kind: Deployment
metadata:
  name: mcp-gong
  namespace: sophia
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mcp-gong
  template:
    metadata:
      labels:
        app: mcp-gong
    spec:
      containers:
      - name: mcp-gong
        image: localhost:5000/sophia-gong:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8080
        env:
        - name: GONG_ACCESS_TOKEN
          valueFrom:
            secretKeyRef:
              name: sophia-secrets
              key: GONG_ACCESS_TOKEN
        - name: GONG_ACCESS_KEY
          valueFrom:
            secretKeyRef:
              name: sophia-secrets
              key: GONG_ACCESS_KEY
        - name: TENANT
          valueFrom:
            configMapKeyRef:
              name: sophia-config
              key: tenant
        resources:
          requests:
            cpu: 100m
            memory: "128Mi"
          limits:
            cpu: 500m
            memory: "512Mi"
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /healthz
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: mcp-gong
  namespace: sophia
spec:
  selector:
    app: mcp-gong
  ports:
    - port: 8080
      targetPort: 8080
  type: ClusterIP