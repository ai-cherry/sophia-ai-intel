apiVersion: apps/v1
kind: Deployment
metadata:
  name: mcp-salesforce
  namespace: sophia
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mcp-salesforce
  template:
    metadata:
      labels:
        app: mcp-salesforce
    spec:
      containers:
      - name: mcp-salesforce
        image: localhost:5000/sophia-salesforce:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8080
        env:
        - name: SALESFORCE_CLIENT_ID
          valueFrom:
            secretKeyRef:
              name: sophia-secrets
              key: SALESFORCE_CLIENT_ID
        - name: SALESFORCE_CLIENT_SECRET
          valueFrom:
            secretKeyRef:
              name: sophia-secrets
              key: SALESFORCE_CLIENT_SECRET
        - name: SALESFORCE_USERNAME
          valueFrom:
            secretKeyRef:
              name: sophia-secrets
              key: SALESFORCE_USERNAME
        - name: SALESFORCE_PASSWORD
          valueFrom:
            secretKeyRef:
              name: sophia-secrets
              key: SALESFORCE_PASSWORD
        - name: SALESFORCE_INSTANCE_URL
          valueFrom:
            secretKeyRef:
              name: sophia-secrets
              key: SALESFORCE_INSTANCE_URL
        - name: TENANT
          valueFrom:
            configMapKeyRef:
              name: sophia-config
              key: tenant
        resources:
          requests:
            cpu: 100m
            memory: "128Mi"
          limits:
            cpu: 500m
            memory: "512Mi"
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /healthz
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: mcp-salesforce
  namespace: sophia
spec:
  selector:
    app: mcp-salesforce
  ports:
    - port: 8080
      targetPort: 8080
  type: ClusterIP