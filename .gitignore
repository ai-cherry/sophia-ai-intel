
# Security - NEVER commit these
.env.vault
.env.services
*.pem
*.key
*_rsa
*_rsa.pub
*_ed25519
*_ed25519.pub
.env

# SSH Keys - NEVER commit private keys
lambda-ssh-key
*-ssh-key
id_*
*ssh*

# SSL Certificates and Keys - NEVER commit private keys
ssl/*.key
ssl/privkey.pem
ssl/private.key
ssl/*.p12
ssl/*.pfx

# Deployment Keys - NEVER commit private keys
sophia-deployment-key
sophia-deployment-key.pub
*deployment-key

# Certificate files that may contain private keys
ssl/*.crt
ssl/*.cert

# Additional sensitive patterns
*.pem
*.key
*_private*
*private_key*

# Node.js
node_modules/
package-lock.json

# TypeScript build artifacts
*.tsbuildinfo

# Python artifacts
__pycache__/
*.pyc

# Temporary or generated files
# Assuming _new.py files are temporary or should be renamed, but will be cautious for now.
*.new
-e 

# Archive directory - historical files
archive/
archive_scan_results/
one_time_scan_results/

# One-time use files
*_FINAL*.md
*_REPORT*.md
*_AUDIT*.md
*_STATUS*.md
*_PLAN*.md
SOPHIA_AI_*.md
DEPLOYMENT_*.md
test_*.py
test-*.py
deploy_*.sh
deploy-*.sh
setup_*.py
setup-*.sh

# Backup files
*.backup
*.bak
*.old
*.orig
*.save
*.swp
*~
*.tmp
*.temp

# Duplicate SSL directory
ssl/local/

# Environment duplicates
.env.production.secure
.env.production.real.backup
.env.production.secure.bak

# Production secrets and sensitive files
*.env.production.real
update_secrets.py
.env*
k8s-deploy/secrets/
kubeconfig*.yaml
*.key
*.pem

# .env.local specifically
.env.local

# Claude-specific development files (not for production)
.claude/
claude-mcp-connector.py
.claude-session/
.mcp-dev/
