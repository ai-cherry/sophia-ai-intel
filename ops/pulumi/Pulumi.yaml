name: sophia-ai-infrastructure
runtime:
  name: python
  options:
    virtualenv: venv
description: Sophia AI comprehensive infrastructure with automated secrets management
template:
  config:
    org:
      description: Pulumi organization
      default: pay-ready
    stack:
      description: Environment stack (dev/staging/prod)
      default: production
